# Natodi Partner API key, created in the admin panel.
# Admin-equivalent and cannot be narrowed to read-only, so it is kept out of the
# repository and never passed into a browser context (it would leak into traces).
# Optional: without it the suite still runs, but a booked appointment is not
# cleaned up automatically and the public API tests are skipped.
NATODI_API_KEY=
